import MainLayout from 'components/shared/main-layout';

const UserProfileFollowing = () => {
  return <MainLayout pageTitle={'User Profile Following'}></MainLayout>;
};

export default UserProfileFollowing;
